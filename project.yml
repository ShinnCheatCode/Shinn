name: 3105

options:
  bundleIdPrefix: com.apple.mobile
  deploymentTarget:
    iOS: "16.0"
  xcodeVersion: "16.0"

settings:
  base:
    SWIFT_VERSION: 5.0
    DEVELOPMENT_TEAM: ""
    IPHONEOS_DEPLOYMENT_TARGET: "16.0"

targets:
  3105:
    type: application
    platform: iOS

    sources:
      - path: ThreeOneOSFive

    info:
      path: ThreeOneOSFive/Info.plist
      properties:
        CFBundleDisplayName: Shinn Cheat
        CFBundleShortVersionString: "2s.0"
        CFBundleVersion: "1"

:    settings:
 []